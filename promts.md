
*"I'm building a devconf (hybrid, in-person + virtual) landing page in plain HTML/CSS only (no JavaScript). My site already has a Banner, Meet the Speakers, Pricing, and Footer section, styled in a light theme with a blue accent.*

*Build me one new section called 'AI Playground' — a bento-grid layout (2 large cards on top: 'Prompt and build, live' with a mock prompt bar, and 'Powered by the newest models' with an icon tile; 3 small cards below: 'Live leaderboard' with a stat + line chart, 'Bring your own stack' with integration icons, and 'Ask a mentor, anytime' with a chat bubble mockup). Use sparkle icons for AI-related cards and a link/chain icon for the integrations card — no generic X/cross icons.*

*Style it with a dark gradient background using #000102 as the base color and #93C5FD as the accent color — write all colors as direct hex/rgba values, no CSS variables (var()). Section height should be 75–80vh.*

*Add a hover effect: when hovering each card, the section's background should crossfade to a relevant photo related to that card's topic, dimmed under a dark overlay so text stays readable — but implement it in a way that avoids any white flash during the transition (don't animate the background shorthand directly across different images; use separate absolutely-positioned layers that only fade opacity).*

*Finally, put the CSS in a separate external .css file instead of an inline <style> tag, and merge any repeated/duplicate CSS rules to keep the file as short as possible."*

--- this AI section is made with Claude.
